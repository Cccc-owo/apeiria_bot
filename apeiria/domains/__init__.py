"""Application-facing domain package.

`apeiria.domains` is currently the temporary use-case layer of the project.
Package-level imports should stay small and stable; implementation helpers in
subpackages should be imported directly only when there is a clear need.
"""

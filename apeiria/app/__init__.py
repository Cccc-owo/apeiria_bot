"""Application layer.

This package owns use-case orchestration. Interface layers such as CLI, HTTP,
and built-in plugins should call into this layer instead of reaching into
infrastructure details directly.
"""
